wheel_base:
  length: 0.405        # m
  width: 0.213         # m
  base_to_rear: 0.2025 # m

limits:
  steering: { inner: 40.0, outer: 40.0 }   # deg
  max_abs_curvature: 2.0                   # 1/m
  velocity: { min: -0.5, max: 0.5 }        # m/s
  acceleration: { min: -2.0, max: +0.6 }   # m/s^2

wheel_radius: 0.055 # m
# Motor to DIFFERENTIAL ratio. To get motor to WHEEL ratio, multiply by 0.5.
gear_ratio: 0.105263158
servo_home_angles: {left: 89, right: 96}  # deg
