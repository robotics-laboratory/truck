#

# [Option 1] Use together with main docker-compose.yaml file:
# docker compose -f docker-compose.yaml -f lidar-net.yaml up -d

services:
  truck:
    networks:
      default:
      lidar-net:

networks:
  lidar-net:
    name: "${CONTAINER_NAME:-truck-${USER}}-lidar-net"
    driver: macvlan
    driver_opts:
      parent: eth0
    ipam:
      config:
        - subnet: 192.168.2.0/24
          gateway: 192.168.2.1
          ip_range: "192.168.2.1${PORT_SUFFIX:-00}/32"
